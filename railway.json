{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "preDeployCommand": "/bin/sh -c \"case \\\"${RAILWAY_SERVICE_NAME}:${SERVICE_ROLE}\\\" in *worker*) echo skipping-worker-migrations ;; *) npm run prisma:migrate ;; esac\""
  }
}
